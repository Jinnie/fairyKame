#ifndef secrets_h
#define secrets_h

/**
 * @file secrets.example.h
 * @brief Template for Wi-Fi Station (STA) mode credentials.
 *
 * Instructions:
 * 1. Copy this file to "secrets.h" in this same directory (code/arduino/include/secrets.h).
 * 2. Uncomment and enter your local 2.4 GHz Wi-Fi credentials below.
 * 3. Note: "secrets.h" is ignored by .gitignore and will never be committed to Git.
 *
 * If "secrets.h" is omitted or these defines are commented out,
 * FairyKame defaults to standalone SoftAP mode (SSID: "MINIKAME").
 */

// #define WIFI_STA_SSID "YourWiFiNetwork"
// #define WIFI_STA_PASS "YourWiFiPassword"
// #define OTA_PASSWORD  "YourOtaPassword"

#endif // secrets_h

